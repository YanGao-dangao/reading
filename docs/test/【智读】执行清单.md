# 【智读】执行清单

> 范围：P0（F01-F25）  
> 技术栈：React + Antd + bun-server + PostgreSQL + bun + Prisma  
> 模式：大需求执行清单（开发 + 联调 + 回归）

## 一、开发清单

- [x] 完成 `TASK-READ-001` 鉴权与租户上下文基建
- [x] 完成 `TASK-READ-002` 搜索 Provider 抽象与 Tavily 实现
- [x] 完成 `TASK-READ-003` 搜索 API 与书籍历史落库
- [x] 完成 `TASK-READ-004` 大纲生成与持久化接口
- [ ] 完成 `TASK-READ-005` 生成任务与 SSE 流式网关
- [ ] 完成 `TASK-READ-006` 历史生成回看接口
- [x] 完成 `TASK-READ-007` React 应用壳与响应式骨架
- [x] 完成 `TASK-READ-008` 搜索页与结果页实现
- [x] 完成 `TASK-READ-009` 大纲树与章节选择交互
- [x] 完成 `TASK-READ-010` 输出模式与心得参数面板
- [x] 完成 `TASK-READ-011` 流式渲染、加载态与复制交互
- [x] 完成 `TASK-READ-012` 历史列表与恢复前端闭环

## 二、联调清单

- [x] 搜索链路联调：`/api/read/search` 在正常与无结果场景均可返回可渲染数据
- [x] 大纲链路联调：选书后可生成并再次读取已持久化大纲
- [ ] 生成链路联调：创建任务后通过 SSE 稳定接收 `token/done/error` 事件
- [ ] 参数联调：知识心得 `style/wordCount` 参数在后端二次校验生效
- [ ] 历史联调：生成成功后可在历史列表看到并恢复内容
- [ ] 多租户联调：跨 tenant 用户无法读取对方书籍/生成记录

## 三、回归清单（对应验收标准）

- [ ] TC-READ-001 ~ TC-READ-004：搜索输入、结果展示、空输入、无结果降级
- [ ] TC-READ-005 ~ TC-READ-007：大纲生成、折叠展开、持久化复访
- [ ] TC-READ-008 ~ TC-READ-011：缩略/详细生成、多章节合并、重生成
- [ ] TC-READ-012 ~ TC-READ-017：知识心得默认/风格/字数/重生成
- [ ] TC-READ-018 ~ TC-READ-020：复制、流式输出、加载与超时提示
- [ ] TC-READ-021 ~ TC-READ-023：书籍历史、内容自动保存、历史回看恢复
- [ ] TC-READ-024 ~ TC-READ-025：桌面与移动端响应式布局

## 四、发布前检查

- [ ] Prisma migration 已执行并可回滚
- [ ] Tavily 与 DeepSeek 密钥已在环境变量中配置
- [ ] JWT 密钥与 Cookie 安全参数（HttpOnly/SameSite/Secure）已配置
- [ ] 服务端日志已开启关键链路埋点（搜索/生成/SSE/鉴权）
