---
description: 当用户以「按研发流程/按 dev 流程/按工程师流程」发起研发任务，或将产品功能清单(03)交给研发实现时，按研发工程师完整流程执行（需求理解确认 → 技术选型确认 → 技术方案 → Task 清单 → 测试 → 自动化验收）
alwaysApply: false
---

# 研发工程师流程

**触发**：消息含「按研发流程/按 dev 流程/按工程师流程」，或用户将 `03-功能清单与验收标准` 交给研发要求实现。  
**输入**：`原型/模块/<模块>/` 下的 03（必须）、02（推荐）、01（可选）、prototype/（推荐）。  
**输出目录**：`研发/模块/<模块>/`

```
研发/模块/<模块名>/
├── 04-技术方案-<模块名>.md              # 整体技术方案
├── 05-Task清单-<模块名>.md              # Task 索引（总览表 + 链接）
├── task/                                 # 原子级 Task 文件夹
│   ├── TASK-<模块缩写>-001-xxx.md
│   └── ...
├── 测试/                                 # 按需创建子目录
│   ├── 06-测试用例-<模块名>.md           # 测试用例文档（映射 03 TC）
│   ├── *.spec.{ts,mjs}                   # 测试脚本（按需分 unit/ integration/ e2e/ 子目录）
│   └── README.md                         # 测试运行说明
└── 验收/
    ├── 验收结果-<模块名>.md
    └── 验收问题表-<模块名>.md
```

### 产物命名规范

- 技术方案：`04-技术方案-<模块名>.md`
- Task 清单索引：`05-Task清单-<模块名>.md`
- 单个 Task：`TASK-<模块缩写>-NNN-<简短描述>.md`（示例：`TASK-READ-001-搜索接口实现.md`）
- 测试用例：`06-测试用例-<模块名>.md`
- TC ID 格式继承 PM 的 `TC-<模块缩写>-NNN`，研发可新增 `TC-<模块缩写>-NNN-U`（Unit）或 `TC-<模块缩写>-NNN-I`（Integration）

| 门禁 | 时机 | 未确认则 |
|------|------|---------|
| **Gate 1** | 输出需求理解后 | 不进入技术选型 |
| **Gate 2** | 输出技术选型后 | 不产出技术方案 |
| **Gate 3** | 验收执行前 | 仅提供手动步骤 |

---

## Phase 0：需求理解确认（Gate 1）

1. 读取 03、02，浏览 prototype/，提取全部功能项按功能域分组。
2. 输出：每个功能域 2-3 句摘要 + 仅对有歧义/不明确依赖/技术风险的项单独列问题 + 全局技术问题（技术栈约束、部署环境等）。
3. **等待用户确认后进入 Phase 1。**

---

## Phase 1：技术选型与方案（Gate 2）

1. **选型**：用户已给技术栈 → 确认式复述；未给 → 对每个决策点列 2-3 个方案对比并推荐。
2. **等待用户确认选型。**
3. **技术方案**：读 `.cursor/engineer-skills/tech-solution/SKILL.md`，产出 `04-技术方案-<模块>.md`（必须含 F 编号 → 技术实现映射表）。产出后软询问「如有调整请说明，否则继续 Task 拆解」（不阻塞）。

---

## Phase 2：Task 拆解

读 `.cursor/engineer-skills/task-list/SKILL.md`，产出：
- `05-Task清单-<模块>.md`（总览表 + 依赖图 + 执行顺序）
- `task/` 每个 Task 独立文件（关联功能 F 编号、TC 编号、依赖项、实现要点、修改文件清单）

**拆解约束**：每条 P0 功能项 ≥ 1 Task；单 Task ≤ 5 文件、≤ 2h；前后端同功能拆为不同 Task；每 Task 至少关联 1 个 TC；依赖无环。

---

## Phase 3：测试规划

读 `.cursor/engineer-skills/test-plan/SKILL.md`，产出：
- `06-测试用例-<模块>.md`：03 中 TC 逐条保持 ID 转化 + 技术补充用例（边界/并发/安全）
- **前端 E2E**（Playwright）：参考 `原型/模块/<模块>/验收/acceptance.spec.mjs`，覆盖主路径、关键分支、加载/流式状态；P0 功能路径覆盖率 100%
- **后端集成测试**：每个 API 端点 ≥ 1 正常 + 1 异常用例；外部依赖 Mock；冒烟链路串联；端点覆盖率 100%，核心逻辑行覆盖率 ≥ 80%
- `测试/README.md`：依赖安装、运行命令、覆盖率报告生成

> **TDD 模式**：用户说「测试先行」则 Phase 2/3 对调，先出测试脚本（RED），再拆 Task 使其 GREEN。

---

## Phase 4：自动化验收（Gate 3）

**前置检查**（执行前向用户回报）：Node/测试框架/覆盖率工具已装、服务已启动、TC 可一一对应、报告路径明确。任一不满足则说明阻塞项。

**用户确认后执行**：运行后端集成测试 + 前端 E2E，汇总覆盖率，产出 `验收结果-<模块>.md`（通过率、失败明细+TC ID、覆盖率汇总）。

**结果处理**：全部通过 → Task 标 ✅；有失败/覆盖率不达标 → 产出 `验收问题表-<模块>.md`（TC ID、关联 Task、预期/实际、严重程度、复现步骤）、Task 标 🔴，询问是否修复。

---

## Task 单独执行模式

用户说「执行 TASK-XXX」时：读 Task 文件 → 确认依赖已 ✅ → 实现代码 → 运行关联 TC → 覆盖率检查 → 更新状态 → 询问「是否继续下一个 TASK-YYY」。

---

## 禁止

- 未过 Gate 1 就出技术方案或写代码
- 未过 Gate 2 就产出完整技术方案
- Task/测试用例脱离 03 功能清单自行发挥
- 验收结果不含覆盖率数据
- 有 F 编号未被 Task 关联，或有 TC 未被测试脚本覆盖
