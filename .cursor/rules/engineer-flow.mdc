---
description: 当用户以「按研发流程/按 dev 流程/按工程师流程」发起研发任务，或将产品需求说明交给研发要求实现时，按研发工程师流程执行（需求理解 → 技术方案 → Task 拆解 → 执行清单 → 编码实现）
alwaysApply: false
---

# 研发工程师流程

## 何时触发

当用户消息满足以下**任一**情况时，本规则生效，必须按研发工程师流程执行：

- 消息含 **「按研发流程」** / **「按 dev 流程」** / **「按工程师流程」**
- 消息含 **「写技术方案」** / **「出技术方案」** / **「拆 Task」** / **「拆任务」**
- 用户将 `docs/product/` 下的需求说明文档交给研发，并要求实现或出方案
- 用户说 **「执行 TASK-XXX」**（进入 Task 单独执行模式）
- 用户通过 **`/dev-flow`** 显式调用

## 不触发时的处理

- 若用户仅咨询技术问题、代码写法、Bug 排查等，按常规对话处理，不强制进入本流程。
- 若用户直接要求修改某个文件或修复某个 Bug（未提及流程/方案/Task），直接执行，不进入 Phase 0 门禁。
- 若用户在已有技术方案的基础上要求「继续编码」或「开始实现」，从 Phase 3 开始，不重走 Phase 0-1。

## 输入与输出

**输入**：`docs/product/` 下的需求说明（必须）、原型 HTML（推荐）。  
**输出**：技术方案 → `docs/engineer/`；执行清单 → `docs/test/`；Task 文件 → `docs/engineer/<功能名>/task/`

---

## 需求规模判定与文件策略

先判定需求规模，再决定产出文件的组织方式：

| 规模 | 判定依据 | 文件策略 |
|------|----------|----------|
| **小** | 单页面改动、1-2 个组件 | **单文件**：在 `docs/engineer/【<功能名>】技术方案.md` 底部追加「执行清单」章节，不单独建执行清单文件，不拆 Task |
| **中** | 跨页面、涉及公用模块 | **双文件**：`docs/engineer/【<功能名>】技术方案.md` + `docs/test/【<功能名>】执行清单.md`；Task 列表写在技术方案内，不单独建 Task 文件 |
| **大** | 跨端、跨模块、架构变更 | **多文件**：技术方案 + 执行清单 + `docs/engineer/<功能名>/task/` 下独立 Task 文件 |

### 文件结构总览

```
docs/
├── product/                                  # 产品输入
│   ├── 【<功能名>】-需求说明.md
│   └── 【<功能名>】-原型.html
├── engineer/                                 # 研发产出
│   ├── 【<功能名>】技术方案.md               # 小需求：含执行清单；中需求：含 Task 列表
│   └── <功能名>/                             # 仅大需求建子目录
│       └── task/
│           ├── TASK-<缩写>-001-xxx.md
│           └── ...
└── test/                                     # 仅中/大需求
    └── 【<功能名>】执行清单.md
```

### 产物命名规范

- 技术方案：`【<功能名>】技术方案.md`
- 执行清单：`【<功能名>】执行清单.md`（小需求合入技术方案，不单独建文件）
- 单个 Task：`TASK-<模块缩写>-NNN-<简短描述>.md`（仅大需求）

---

## 门禁

| 门禁 | 时机 | 未确认则 |
|------|------|---------|
| **Gate 1** | 输出需求理解后 | 不进入技术方案 |
| **Gate 2** | 输出技术方案后 | 不拆解 Task / 不写代码 |

---

## Phase 0：需求理解确认（Gate 1）

1. 读取 `docs/product/` 下的需求说明，浏览原型（如有），提取功能项按功能域分组。
2. 判定需求规模（小/中/大），告知用户将采用的文件策略。
3. 输出：每个功能域 2-3 句摘要 + 仅对有歧义/不明确依赖/技术风险的项列出问题。
4. **等待用户确认后进入 Phase 1。**

---

## Phase 1：技术方案（Gate 2）

1. 读取 `.cursor/skills/tech-design-doc/SKILL.md`，按 skill 中的规模判定规则确定章节范围。
2. 若有技术选型则给出方案，等待用户确认技术选型。
3. 读取相关代码文件（组件、API、类型、权限等），确保方案描述与当前实现一致。
4. 产出 `docs/engineer/【<功能名>】技术方案.md`。
5. 产出后询问「方案如有调整请说明，否则继续 Task 拆解」（不阻塞）。

---

## Phase 2：Task 拆解与执行清单

根据规模产出不同粒度的任务拆解：

**小需求**：
- 在技术方案文件末尾追加「## 执行清单」章节（开发 + 联调 + 回归，checkbox 格式）。
- 不创建独立 Task 文件和执行清单文件。

**中需求**：
- 在技术方案内追加「## Task 列表」章节，以表格列出各 Task（编号、描述、关联功能、涉及文件、状态）。
- 单独产出 `docs/test/【<功能名>】执行清单.md`（开发 + 联调 + 回归三段 checkbox）。

**大需求**：
- 在 `docs/engineer/<功能名>/task/` 下为每个原子任务创建独立文件。
- 每个 Task 包含：关联功能编号、依赖项、实现要点、涉及修改文件清单。
- 单独产出 `docs/test/【<功能名>】执行清单.md`。

**通用拆解约束**：
- 每条功能项 ≥ 1 Task（或执行清单项）
- 单 Task ≤ 5 文件、≤ 2h 工作量
- 前后端同功能拆为不同 Task
- 依赖无环

---

## Phase 3：编码实现

进入编码前，读取技术方案中的影响组件清单，逐文件实施变更。

**执行规则**：
- 每次变更前先读取目标文件，确认当前实现。
- 修改完成后检查 linter 错误并修复。
- 按执行清单顺序推进，完成一项勾选一项。
- 实现过程中发现方案遗漏，先更新方案文档再改代码。

---

## Task 单独执行模式

用户说「执行 TASK-XXX」时：读 Task 文件 → 确认依赖项已完成 → 实现代码 → 更新 Task 状态 → 询问「是否继续下一个 TASK-YYY」。

---

## 禁止

- 未过 Gate 1 就出技术方案或写代码
- 未过 Gate 2 就拆解 Task 或写代码
- Task / 执行清单脱离需求说明中的功能清单自行发挥
- 技术方案中描述与当前代码实现不一致却不加标注
- 有功能项未被 Task 或执行清单覆盖
